import React from 'react';

const SectionParagrah = ({text}) => {
    return (
        <p className="text-[10px] md:text-[13px] lg:text-[15px] my-7 flex flex-row font-light ">{text}</p>
    );
}

export default SectionParagrah;
